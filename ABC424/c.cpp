#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, R, closed, opened;
    cin>> N, R;
    vector<int> L(N);

    for(int i = 0; i<N; i++){
        cin>>L[i];
    }

    
    for(int i = 0; i<N; i++){
        if(L[i]==0){
            closed++;
        }
    }
    // going left
    if()
    //going right 
    if(R) for(int i = R)
/*
    close operations which is at least 4
    if you open the door it's +1 * 2 
*/
