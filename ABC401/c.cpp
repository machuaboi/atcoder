#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i =0; i<n; i++)

int main(){
    int n, k; 
    cin >> n >> k;
    vector<int> a(n+1);
    vector<int> s(n+2);

    rep(i,k) a[i] = 1;
    rep(i,k)


    return 0;
    
}c